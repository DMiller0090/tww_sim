# Regenerable artifacts (captures, viz, DTMs, run dumps, logs)
_generated/
harness/capture/*.csv

# Local-only research notes / session provenance (not part of the shared repo)
_notes/

# Local-only agent/editor tooling (kept out of the shared repo)
.claude/
CLAUDE.md

# Python
__pycache__/
*.pyc

# Packaging / editable-install artifacts
*.egg-info/
build/
dist/

# Large recorded savestates (never track)
*.dtm.sav

# Game savestates — dumps of copyrighted game RAM; never distribute. Provide your own locally.
*.s10
*.sav
fixtures/savestate/
# Test-owned DTM anchors are local copyrighted RAM; the README + convention are tracked, the
# .sav files are not (regenerate locally with harness/dtm/capture_anchor.py).
!tests/dolphin/anchors/README.md

# Session handoffs are local working notes, never tracked (see _notes/)
/HANDOFF.md

# Machine-specific Dolphin/ISO paths — never tracked. Copy dolphin.local.example.json
# to dolphin.local.json and edit for your machine (see harness/dolphin_env.py).
/dolphin.local.json

# Cython build artifacts (regenerate with _build_fpc.py). The .pyx source IS tracked; the
# compiled .pyd (platform-specific) and generated .c are not. fp.py falls back to pure-Python
# ctypes when the compiled _fpc is absent (bit-identical, just slower).
tww_sim/**/*.pyd
tww_sim/**/*.c
build/
